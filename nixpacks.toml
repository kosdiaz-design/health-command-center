[providers]
name = "node"

[phases.setup]
nixPkgs = ["nodejs_20", "python3", "gcc", "gnumake"]

[phases.install]
cmds = ["npm install --include=optional"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node dist/index.cjs"
